package mezz.jei.ingredients;

/**
 * ESBOCO DE COMPILACAO - nunca empacotado; veja o Internal.java ao lado.
 *
 * <p>Os campos que o gancho le/troca ({@code combinedSearchTrees}, nao-final, e
 * {@code elementList}, final so de leitura) sao acessados por REFLEXAO com o nome em texto,
 * entao nao precisam constar aqui - a secao 22 do verify.sh confere os dois no jar real.
 */
public class IngredientFilter {

    /** javap: public void invalidateCache() */
    public void invalidateCache() {
        throw new UnsupportedOperationException("esboco de compilacao");
    }
}
