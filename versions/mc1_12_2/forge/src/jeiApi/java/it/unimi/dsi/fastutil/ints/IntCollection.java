package it.unimi.dsi.fastutil.ints;

/**
 * ESBOCO DE COMPILACAO - nunca empacotado. O fastutil vem do ambiente que ja roda o JEI.
 *
 * <p>Marcador vazio: existe so para o descritor de {@code IntSet.addAll(IntCollection)}
 * sair identico ao que o proprio JEI emite (conferido no bytecode do CombinedSearchTrees).
 */
public interface IntCollection {
}
