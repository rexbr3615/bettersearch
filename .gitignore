# ------------------------------------------------------------------------------
# O que o Git deve FINGIR QUE NAO EXISTE. Tudo aqui nasce do build ou da maquina
# de quem compila - se apagar, o gradlew recria. Historico e so para o que e
# escrito por gente.
# ------------------------------------------------------------------------------

# Compilados e caches do Gradle (em qualquer nivel: raiz e cada modulo)
build/
.gradle/

# Pastas de "rodar o jogo" que o Loom/ModDev criam nos modulos
run/
runs/

# O pacote de entrega que viaja entre a IA e o rivalzin - nao e codigo-fonte
bettersearch-1.3.0.zip

# Coisas de editor/IDE
.idea/
.vscode/
*.iml

# Lixo de sistema operacional
Thumbs.db
.DS_Store

# ------------------------------------------------------------------------------
# Ferramental interno da dupla (rivalzin + IA). Roda so na nossa maquina e
# descreve COMO a gente trabalha e publica - nao e parte do mod, e o publico
# do GitHub nao precisa ver. Lembrete: o manto so esconde o que o Git ainda
# nao fotografou; o que ja foi commitado um dia precisa ser apagado da pasta
# do repositorio UMA vez (e o commit dessa exclusao enviado) para sumir de la.
# ------------------------------------------------------------------------------
*.ps1
/ferramentas/
/tools/
/PORTING.md
